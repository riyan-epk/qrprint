{
  "name": "qrprint",
  "version": "1.0.0",
  "description": "Self-service QR print system — server + shop dashboard + provider kill-switch. V1 runs fully local, no cloud account needed.",
  "type": "module",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "setup": "node scripts/setup.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "helmet": "^7.1.0",
    "multer": "^2.0.1",
    "pdf-lib": "^1.17.1",
    "qrcode": "^1.5.3"
  }
}
